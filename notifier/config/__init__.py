# Instance-status helpers that used to live here duplicated the WhatsApp
# Instance controller and the WuzAPI-backed notifier.api. Use
# notifier.api.check_instance_status / reconcile_instance_status instead.
